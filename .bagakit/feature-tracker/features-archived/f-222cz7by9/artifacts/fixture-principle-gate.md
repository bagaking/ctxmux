# Wrong-case fixture implementation gate

- protected_goal_or_principle: every retained real-world case has a deterministic disposition, and every currently applicable P0 invariant is continuously proven through ctxmux's public or owner-owned boundary.
- project_native_strategy: keep one JSON corpus as machine traceability; extend existing Rust unit/integration tests, TypeScript tests, generated-schema check, and `scripts/check.sh`; use only standard libraries and dependencies already present.
- failure_boundary: do not create a generic fixture runner, plugin system, scheduler, skipped-test graveyard, or second lifecycle. Future capability cases remain validated inactive records. Do not freeze unresolved characterization behavior as the desired contract or run untrusted source code.
- proof_plan: corpus validation rejects missing sources, choices, activation owners, and test refs; active/covered cases point to checked-in tests; public lifecycle and wire fixtures pass under `scripts/check.sh`; researcher doctor and Feature Tracker gate preserve evidence parentage.

The first ladder rung that satisfies the goal is project-native wiring plus narrow tests. New abstractions or dependencies are not justified.
